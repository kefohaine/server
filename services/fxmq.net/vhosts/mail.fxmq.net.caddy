# mail.fxmq.net — Roundcube webmail (Docker Mailserver platform).
# TLS: Let's Encrypt DNS-01 via Cloudflare (same pattern as every vhost).
# The mail container reuses this cert for SMTP/IMAP TLS (SSL_TYPE=manual,
# caddy_data mounted read-only at /caddy), so renewals are shared.

https://mail.fxmq.net {
    tls {
        dns cloudflare {env.CF_API_TOKEN}
    }
    import compress
    request_body {
        max_size 25m
    }
    header {
        import security_headers
    }
    reverse_proxy 172.22.0.10:80 {
        # strip roundcube's own security headers so the site-level set yields one value
		header_down -X-Frame-Options
		header_down -X-Content-Type-Options
		header_down -Referrer-Policy
    }
}
