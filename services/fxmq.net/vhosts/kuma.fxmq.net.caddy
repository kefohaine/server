# kuma.fxmq.net — decommissioned stub: every path responds plain "ok".
# (Uptime Kuma retired from the edge 2026-08-28.)
# TLS: Let's Encrypt DNS-01 via Cloudflare.

https://kuma.fxmq.net {
    tls {
        dns cloudflare {env.CF_API_TOKEN}
    }
    respond "ok" 200
}
