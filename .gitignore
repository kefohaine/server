.DS_Store
__pycache__/
*.pyc
services/cloud/.env
services/nextcloud/.env
services/mailserver/.env
services/vaultwarden/.env
# Runtime data dirs — canonical home is /var/www/custom/projects/homelab/
# (siblings of repo/), never inside the repo. Ignored so a stray copy can't
# be committed (2026-08-29 cleanup removed strays that were created in repo/).
/caddy_data/
/cloud/
/kuma/
/vault/
/download/
/backups/
/puffer/
/open-webui/
/mail/
/mailserver/
/pgdata/
/talk/
.claude/
